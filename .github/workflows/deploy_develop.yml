name: EB deploy
on:
  push:
    branches: [ develop ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ develop.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ develop.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ${{ develop.AWS_DEFAULT_REGION }}
    steps:
      - uses: actions/checkout@v2
      - name: Install Python 3.11
        uses: actions/setup-python@v2
        with:
          python-version: 3.11
      - name: Install EB CLI using pip
        run: |
          python -m pip install --upgrade pip
          pip install awsebcli
      - name: Init to Elastic Beanstalk
        run: |
          eb init
      - name: Deploy to Elastic Beanstalk
        run: |
          eb deploy
